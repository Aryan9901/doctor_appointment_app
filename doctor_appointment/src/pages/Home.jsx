const Home = () => {
	return (
		<main className="mt-[3.4%] w-full min-h-screen">
			<section
				className="hero-section w-full h-[130vh] bg-[url(/heroimg.png)] bg-cover  mx-auto"
				style={{ borderRadius: "2rem" }}
				// style={{ borderTopLeftRadius: "2rem", borderTopRightRadius: "2rem" }}
			></section>
		</main>
	);
};

export default Home;
